# NVIDIA / CUDA fallback. The pipeline treats a CUDA device exactly like an
# XPU one (bf16 autocast, pin_memory, device seeding) — pass --device cuda.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.4
torchvision>=0.19

timm==1.0.15
numpy>=2.0
pillow>=11.0
scikit-learn>=1.5
pytorch-wavelets==1.3.0
PyWavelets>=1.7
huggingface-hub>=0.26
safetensors>=0.4
pyarrow>=17
PyYAML>=6.0
tqdm>=4.66
python-docx>=1.1
