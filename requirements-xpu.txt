# Intel Arc iGPU (XPU) — the environment this project was trained on.
# torch 2.13+xpu hangs on the first oneDNN GEMM with the Nov-2024 Arc driver;
# 2.6.0+xpu is the last version that runs. Install torch/torchvision from the
# Intel XPU wheel index:
#   pip install torch==2.6.0+xpu torchvision==0.21.0+xpu \
#       --index-url https://download.pytorch.org/whl/xpu
torch==2.6.0+xpu
torchvision==0.21.0+xpu

timm==1.0.15
numpy==2.5.2
pillow==12.3.0
scikit-learn==1.9.0
pytorch-wavelets==1.3.0
PyWavelets==1.9.0
huggingface-hub==1.28.0
safetensors==0.8.0
pyarrow==25.0.1
PyYAML==6.0.3
tqdm==4.70.0
python-docx==1.2.0
